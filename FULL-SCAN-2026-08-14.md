# Full CRM Scan — 2026-08-14

## Critical deployment finding
The newest two production deployment attempts failed because a stale GitHub file remained after manual ZIP uploads:

`app/(crm)/clients/import/ClientImportForm.tsx`

It imported functions from the retired Cognito helper modules. The previous cleanup release intentionally made those modules inert, so the stale component failed TypeScript.

This release includes an inert replacement `ClientImportForm.tsx`, ensuring the stale GitHub path is overwritten rather than relying on deletion.

## Live production
The most recent successful production deployment remains `import button 2`.
The two newer `opt` deployments did not go live.

## Database integrity
Checked:
- 420 clients
- 338 documents
- 264 life-insurance rows
- 173 banking rows
- 4,789 audit rows
- 1 website lead
- 3 profiles
- 0 clients missing first/last name
- 0 clients missing assigned agent
- 0 completely blank life-insurance rows
- 0 orphan documents
- 0 orphan life-insurance rows
- 0 orphan banking rows

All 14 public tables have Row Level Security enabled.

## Supabase advisors
Performance: INFO-only unused-index notices. No critical performance lints.
Security: leaked-password protection is disabled.

## Runtime
Only one error group in the last 24 hours, from an older deployment:
website-lead intake missing server-side Supabase credentials.
No evidence in this scan that it is recurring on the current successful deployment.

## Source/security scan
- No hard-coded JWTs found.
- No hard-coded Supabase service-role secret found.
- No `eval`, `new Function`, or `dangerouslySetInnerHTML`.
- No TODO/FIXME/HACK markers in application TypeScript.
- Cognito runtime helpers remain inert; original code is preserved only in the archive ZIP.

## Performance observations
- Largest server data module: `lib/medical-qualifications.ts` ~164 KB.
- Global CSS ~73 KB.
- Company contacts ~53 KB.
- Apple document parser ~28 KB.
- Document import UI ~27 KB.
These sizes are acceptable at the current CRM scale; the large medical dataset is not a reason to alter behavior now.
- Client detail uses several `.select('*')` queries. They run in parallel and current table sizes are small. Explicit column selection is a future micro-optimization, not an urgent bottleneck.

## Recommendation
Do not drop the unused indexes yet. Current data volume is small and some indexes support future search/filter growth.
