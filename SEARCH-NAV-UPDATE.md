# Search + Navigation Update

- Added PostgreSQL `pg_trgm` GIN indexes for partial searches on client first name, last name, phone and email.
- Added a sidebar link to `https://mayerig.com` that opens in a new tab.
- Made the bear in the desktop sidebar and mobile/top header link directly to `/dashboard`.
- The Supabase search optimization has already been applied to the live project.
